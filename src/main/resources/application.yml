server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://localhost:5332/student
    username: simonecuccaro
    password: pass
  jpa:
    database: POSTGRESQL
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  main:
    web-application-type: servlet